============
Installation
============

At the command line::

    $ easy_install django-sessioninfo

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-sessioninfo
    $ pip install django-sessioninfo